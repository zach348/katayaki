<!DOCTYPE html>
<html>
  <head>
    <title class='orange-shadow'>Katayaki</title>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <header class="header">
      <div class='row' id='kat-headline'>
        <h1 class="headline">Katayaki</h1>
      </div>
      <div class='row' id='kat-subnav'>
        <ul class="header-subnav">
          <%- if user_signed_in? -%>
          <li class="navItem"><%= link_to 'Katayakies', user_path(current_user) %></li>
          <li class="navItem"><%= link_to 'Circles', groups_path %></li>
          <li class="navItem"><%= link_to 'Rate', rate_path %></li>
          <li class="navItem"><%= link_to 'Aspire', goals_path %></li>
          <li class="navItem"><%= link_to 'Trending', static_pages_path(get_splash: true) %></li>
          <li class="navItem"><%= link_to 'Account',  edit_user_registration_path(current_user)%></li>
          <li class="navItem"><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></li>
          <%- else -%>
          <li class="navItem"><%= link_to 'Sign Up', new_user_registration_path  %></li>
          <li class="navItem"><%= link_to 'Sign In', new_user_session_path  %></li>
          <li class="navItem"><%= link_to 'Trending', static_pages_path %></li>
          <%- end -%>
        </ul>
      </div>
    </header>

    <div class='row'>
      <% flash.each do |key, value| %>
        <p class='small-8 small-centered large-4 large-centered columns'id="flash"><%=value%></p>
      <% end %>
    </div>
    <div className="wrapper">
      <%= yield %>
    </div>
  </body>
</html>
